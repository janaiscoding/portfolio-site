type WatermarkProps = {
  title: string;
  top: string;
  left: string;
};
type ButtonProps = {
  content: string;
  linkTo: string;
  selector: string;
};

export type { WatermarkProps, ButtonProps };
